modules:

  - type: dnf
    repos:
      copr:
        - vorona/aws-rpm-packages
    optfix:
      - awsvpnclient
    install:
      packages:
        - awsvpnclient

  - type: systemd
    system:
      enabled:
        - awsvpnclient
